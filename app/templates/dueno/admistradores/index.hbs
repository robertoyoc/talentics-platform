{{link-to 'nuevo' 'dueno.admistradores.nuevo'}}

<table style="width:100%">
  <tr>
    <th>nombre</th>
   <th>apellido</th> 
   
  </tr>
  {{#each model as |cuenta|}}
  <tr>
    <td>{{cuenta.nombre}}</td>
    <td>{{cuenta.apellido}}</td> 
    <td>{{link-to 'editar' 'dueno.admistradores.editar'cuenta.id}}<button {{action 'delete' cuenta}}>borrar</button></td>
  </tr>
  {{/each}}
</table>
