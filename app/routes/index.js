import Route from '@ember/routing/route';

export default Route.extend({
	beforeModel(){
		
		// this.store.createRecord('account', {
		// 	nombre: 'Ale',
		// 	apellido: 'DLTI',
		// 	uIdPerfil: 'idk',
		// }).save()
			
	}
});
